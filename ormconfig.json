{
  "type": "postgres",
  "port": 5432,
  "host": "localhost",
  "username": "ignite",
  "password": "docker",
  "database": "rentalx",
  "migrations": ["./src/database/migrations/*.ts"],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
